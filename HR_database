1)Display all details of employees 
Ans: 
Select * from emp;
2)Display employee name and salary 
Ans:
select ename and sal from emp;
3)display name and department number 
Ans:
Select ename,deptno from emp;
4)Display distinct job from table
Ans:
select distinct job from emp;
5)display employee who belong to dept 30 
Ans:
select ename from emp where dept no 30;
6)Display employes whose salary greater then 2000
Ans:
select ename from emp where sal>2000;
7)Display employee hired after 01-jab-1981
Ans:
 select ename from emp where hiredate>STR_TO_DATE(’01-JAN-1981’);
8)Display employee name starting with S.
Ans:
    Select ename from emp where ename like =’S%’;
9)Display whose receive commission 
Ans:
    Select ename from emp where com is not null;
10)Display employee whose dept. no 10 or 20
Ans: 
        select ename from emp where deptno in (10,20);
11)Display employee sorted by salary in desc
Ans:
       select ename from emp ordered by desc;
12)display ename ,sal com sorted  by name .
Ans: 
        select ename, sdal,com from emp ordered by ename;


13)select to display employee with no manager 
Ans:
      Select ename,job from where mgr is null;
14)Display  the location of department 20 
Ans:
         Select loc from dept where deptno =20;

15)Display dept details sorted by dept name 
Ans:
       Select * from dept order by dname;
16)Display employees where salary is between 1000 and 3000
Ans:
      Select ename from emp where  sal between 1000 and 3000;
17)count total no of employee
Ans:
      Select count(*) from emp;
18)Display minimum and maximum salary from emp
Ans:
       Select min(sal),max(sal) from emp;
19)Display total number of employees for each job 
Ans :
      Select job,count(*) emp group by job;
20)Display current system date?
Ans:
SELECT TO_CHAR(SYSDATE, 'DD-MM-YYYY') FROM DUAL;
21)Display ename, sal and yearly sal?
 Ans:
       Select ename,sal(sal*12) as year_sal from emp;
22)Display employees joned in 1981 
Ans:
     SELECT ename FROM emp WHERE joined_year=’1991’;
23)Display emp details sorted by hiredate?
Ans:
     Select ename from emp order by hiredate desc;
24)Display average salary of each depertment?
Ans:
       Select deptno,avg(sal) from emp group by deptno;
25)Display ename along with department name with join ?
Ans:
     Select emp.ename,dept.dname from emp join dept;
26)Display emp earning more than average of company?
Ans:
      Select ename from emp where sal>(select avg(sal) from emp);
27)Display employes who have commission in each department?
Ans: 
       Select dept_no,ename from emp ehere com>0;
28)Display employees whose name contain A?
Ans:
      select ename from emp ename like ‘%a%’;
29)Display highest salary of each job.
Ans:
        select job,max(sal) from emp group by job;
30)Display employees whose work in department in NEWYORK.
Ans:
       select e.ename, e.deptno, d.dname from emp e inner join dept d on e.deptno = d.deptno wherwe d.loc=”New York” ;
31)Display employees whose were hired on Monday
Ans:
      Select e_name from emp where weekday(hioredate)=’monday’ ;
32)Count the no of employees who do not receive commission 
Ans:
          select count(*) from emp where com is NULL;
33)Display job along with total commission for each job
Ans:
         select job,sum(com) from emp group by job;
34)Display department with more then 3 employee
Ans:
     select dname from dept where deptno in(select emp dept no from emp group by dept no having count(*)>3);
35)Display employees who draw second highest salary.
Ans:
       select ename, sal from emp order by desc limit 1,1;
36)Display employes whose have same job as SMITH.
Ans: 
      select ename from emp where job =(select job from emp where ename=”SMITH”)
37)Display each employee salary grade 
Ans:
    select e.ename, s.grade from e join slarygrade s;

38)display the employee name,salary,salgarade 
Ans:
    select e.name,s.grade from emp e join salgrade s;
39)Display managers
Ans:
       select ename from emp where job=’ manager’;
40)Display employee who earn highest in department 30
Ans:
       select ename from emp where deptno=30   and sal =(select max(sal) from emp where dept no=30);
41)Display the employee with highest salary
Ans:
     select ename from emp where sal=(select max(sal) from emp);
42)Display employees who earn more then avg in the dept
Ans:
    select ename from emp e where sal >(select avg(sal) from emp where dept no =e.deptno);
43)Display department having maximum total salary paid to emp
Ans:
      select dept_no, sum(sal) from emp group by dept no order by total_sal desc limit 1;
44)Display employees joined  before their managers
Ans:
      select e.ename from emp e joinbemp m on e.mgr m.empno where e,.hiredate < m.hiredate;
45)Display department where no employees
Ans:
      select dname from dept deptno not in(select distinct deptno from emp);
46)Display third highest salary
Ans:
     select distinct sal from emp order by sal limit 2 1;
47)Display count of employees hired month wise 
Ans:	
     select month name(hiredate), count(emp_no) from emp group by mont name(hire date);
48)Display employes joined after ford
Ans:
     select ename from emp where hiredate>(select hiredate from emp where ename=”FORD”);
49)Display employees whose dept is not precent
Ans:
      select ename from emp where dept no not in (select dept no from dept);
50)Display job along with diff between max and min salary.
Ans:
      select job max(sal) -min(sal) as s-d from emp group by job;
51)Display employee names along with length of name 
Ans:
      select ename,length(ename) from emp;

52)Display salary formatted to two decimal place
Ans:
      select round(sal,8) from emp;
53)Display each employee with masked
Ans:
     select ename,concat(left(ename,3) repet (‘*’ length (ename)-3)) from emp;
54)Display ename &replace commission
Ans:
       Select ename,infull(com,’No commission’) from emp;
55)Create view to show employee details 
Ans:
     create view emp_view as select e_name ,e_sal ,d.dname from emp e join dept  d on e.dp = d.dp;
56)update salary of employee
Ans:
    update emp set sal  = sal * 1.10 where dept no = 30;
57)Remove employees with no department
Ans:
      Delete from emp where dept no is null;
58)Insert data in dept table
Ans:
     insert into dept values(50,’ HR’);   
 59)Display employee working same department as BLAKE
Ans:
      select ename from emp where dept_no =(select deptno from emp where ename=’BLAKE’
60)Display employees whose job appears more than once.
Ans:
      select ename,job from emp where job in (select job from emp group by job having count(*)>1);
61)Display employee whose salary higher then maximum salary
Ans:
      select ename from emp where sal>(Select max(sal) from emp where dept no=10);
62)Display employee whose salary higher than maximum salary 
Ans:
     Select ename from emp  where sal>(select max(sal) from emp where dept no=30);
63)Display department with commission 
Ans:
     select deptno from emp grouyp by dept no having min(com)>0;
64)Display employee salary the average salary of that own department
Ans:
     select e.ename,e.deptno,e.sal from e where e.sal =(select avg(sal) from emp where deptno=e.deptno);
65)Display employee & job salary lower then average salary job
Ans:
     select job,ename from emp where sal<(select aveg(sal) from empe where e.job=job);
66)Display employee with minimum salary in their job 
Ans:
     select job,ename from emp where sal in (select min(sal) from emp e where e job = job group by job);
67)Display employees working in same location as KING
Ans:
     select e.ename from emp e where e.deptno in (select d.dptno from dept d where d.loc =(select d2 loc from emp2 join dept d2 in d2.deptno = e2.deptno where e2.name=’ KING’ ))
68)Display employees whose salary is not within salary range
Ans:
     select ename,sal,grade from emp ,salgrade where sal not between(select min(sal) from salgrade) and (select max(fisal) from salgrasde)
69)Find the second heights in each dept 
Ans:
    select e.ename, e.sal, e.deptno from emp where sal = (select (distinct set from emp where depno = e.deptno order by sal does limit 1,1
70)Display emps dept has at least one manager
Ans:
      select e.ename, e.deptno emp e where e.dept no in (select deptno from emp where job=’manager’); 
71)Select depertment with no manager
Ans:
    select dept_no, dept_name from dept where deptno not in(select dept no from emp where job = “Manager”)
72)Display employee who earn a salary that false into the highest grade
Ans:
     select ename,sal from emp where sal between (select losal from sal grade where grade =(select max(grade) from salary grade) and (select hisal from salgrade where grade = (select max(grade) from sal grade);
73)display employes who have same salary some one in different department
Ans:
     select e1.name,e1.dept no from emp e1 where exists (select 1 from emp e2 where e1.sal= e2.sal and  e1.deptno =e2.dept no);
74)Display the employee who has earliest hiredate in each dept.
Ans:
    select ename, deptno, hiredate from empe where hiredate =( select min(hiredate) from emp where deptno = e.deptno);
75)employee less then average number of employees 
Ans:
     select depot from emp group by dept no having count(*) < (select avg(cnt) from (select count(*)  as ent from emp group by dept no);
76)Display employee whose managers work in different location 
Ans:
     select e.ename, m.mnae from emp e join m on e.mar m.empno join dept d on e.deptno d i.deptno join dept d2 on m.deptno = d2.deptno where d1.loc ! = d2.loc ;
77)Display employee who work in a dept where total dsalary is greater then all other depts
Ans:
    select ename,sal from emp where deptno=(select deptno from emp by deptno orderby sum(sal) desc limit 1);
78)Employee earning greater than average salary of all managers
Ans:
     select e.ename,d.dname from emp ejoin dept d on e.deptno d.deptno where e.sal>(select avg(sal) from emp where job= manager);
79)Display employee earning more than their manager
Ans:
    select d.dname, e.ename from emp e join emp m on e.mgr = m.empno join dept d on e.deptno = d.deptno where e.sal>m.sal
80)Display employee & dept whose salary is within salary grade 3
Ans:
     Select e.ename, e.sal, d.dname, from emp e join dept d on e.deptno = d.deptno join salary grade s on s.grade = 3 where e.sal between s.losal and s.hisal;
